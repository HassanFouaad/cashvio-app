---
title: Receipt Settings
description: How to customize your receipt content, control what appears on it, and enable auto-printing.
---

## What are Receipt Settings?

Receipt settings control the **printed and digital receipt** that accompanies each order: what appears on it and whether it prints automatically after each sale.

## How to Configure Receipt Settings

1. Go to **Settings > Stores**, open your store, and select the **Receipt** tab. (With one store, the Settings hub's "Receipt Settings" link takes you straight there.)
2. Adjust the settings and click **Save**.

## What You Can Customize

| Setting | Description |
|---------|-------------|
| **Printer** | Pair a thermal printer, run a **Test print**, or **Open cash drawer** before a live sale |
| **Show Header** | Whether to show a custom header section at the top |
| **Header Text (English / Arabic)** | Custom header text in both languages, shown when the header is on |
| **Footer Text (English / Arabic)** | Custom footer text in both languages (e.g., "Thank you for your visit!"); required |
| **Show Logo** | Whether to include your business logo |
| **Show Store Address** | Whether to print the store's address |
| **Show Store Phone** | Whether to print the store's phone number |
| **Auto Print After Order** | Automatically print the receipt when a sale is completed on the [New Sale screen](/docs/orders/creating-orders) |

<Callout title="Note">
The store name, address, and phone values come from your [store settings](/docs/stores/creating-store); here you only choose whether to show them. The receipt is rendered in the language you are using (English or Arabic), with your bilingual header/footer texts.
</Callout>

## Pair and Test Your Printer

Set up the printer on the **Receipt** tab so you are not choosing a device mid-sale:

1. Open the **Printer** card at the top of Receipt settings.
2. Tap **Pair printer** and select your USB or Bluetooth COM thermal printer.
3. Tap **Test print** to send a sample labeled `TEST-PRINT` (not a real order).
4. Optionally tap **Open cash drawer** to confirm the kick cable.

See [POS Cashier Shortcuts](/docs/orders/pos-cashier-shortcuts) for the same open-drawer action on **New Sale**.

## Printing Receipts

You can print a receipt from the sale success screen or from any order's details:

1. Open the **Order Details** page and click **Print / Share Receipt**.
2. Choose **Print**. The first time, you pick the print method:
   - **Thermal printer**: prints directly to a connected receipt printer
   - **Browser print**: opens the standard print dialog
3. Your choice is remembered per store (you can reset it later).

The same dialog also offers **PDF download**, a **shareable receipt link**, and a **QR code** for the customer.

<Callout title="Tip">
Enable **Auto Print After Order** for counter sales so the receipt prints itself the moment each sale completes. Pair the printer first so auto-print does not pause for a device chooser.
</Callout>
